name: my first workflow
on: push

jobs:
 first_job:
       runs_on: ubuntu-latest
       steps:
        - name: welcome message
          run: echo "welcome friends in github actions"
        
        - name: list all files
          run: ls -a
          