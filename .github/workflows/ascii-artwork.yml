name: my first workflow

on: push

jobs:
  first_jobs:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v6
      
      - name: welcome message and list files
        run: |
            echo "welcome to github actions"
            ls
      - name: ascii artwork install cowsay
        run: sudo apt install cowsay -y
      - name: execute cowsay command
        run: cowsay -f crocodile "i am trying to eat you" >> crocodile.txt
      - name: test file exists
        run: grep -i "crocodile" crocodile.txt
      - name: read file
        run: cat crocodile.txt
      - name: list repo files
        run: ls -lta


      
      
        